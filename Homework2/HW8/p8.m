r = 2.79e-2;
i = 13.8e-3;
d = 3 * r;
B = .83;
Ra = 2 * r;
Ba = mu0() * i / (2 * pi * Ra);
t = mu0() * i / (2*r);
d1 = mu0() / (2 * pi * r);
d2 = .83 * mu0() / (6 * pi * r);
I2 = (.83 * t + Ba) / (d1 -d2);
disp(I2*10^3);